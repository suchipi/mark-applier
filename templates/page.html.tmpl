<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <%= title ? `<title>${_.escape(title)}</title>` : "" %>
    <style>
      <%= css %>
    </style>
  </head>
  <body>
    <%= checkboxHtml %>
    <article class="markdown-body">
      <%= content %>
    </article>
  </body>
</html>
